import { CheckoutForm } from "@/components/checkout/checkout-form";

export const metadata = {
  title: "Checkout",
  description: "Secure checkout placeholder for Naughty Fantasy orders.",
};

export default function CheckoutPage() {
  return <CheckoutForm />;
}
